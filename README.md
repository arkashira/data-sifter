# Data Sifter

A visual workflow builder for data scraping.

## Usage

1. Create a new workflow by adding blocks to the canvas.
2. Connect blocks to create a workflow.
3. Save the workflow to a JSON file.
4. Load the workflow from the JSON file to execute it.
